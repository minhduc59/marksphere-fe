node_modules
.next
.git
.gitignore
.vscode
.idea
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
coverage
README.md
Dockerfile
.dockerignore
# NOTE: do NOT ignore .env — CodeBuild writes NEXT_PUBLIC_* into it
# and `next build` must see them to inline them into the client bundle.
